import { Bell } from "lucide-react"
import { Button } from "../common/Button"

export const SectionTop = ({name}) => {
    return(
        <>
            <div className="section-top">
                <h2>{name}</h2> 
            </div>
        </>
    )
}
